package com.xylotune.app.ui

// What the lyrics button on AppToolbar cycles through, in order. Roll is the default —
// composing, practising and playing all happen there; the other three trade the keys (or
// the paper, for Sheet/Link) for room to read, fix, or link the song.
enum class LyricsView { Roll, Sheet, Link, Keys }
