<resources>
    <!-- Pre-Compose window background only; Compose (MaterialTheme in ui/theme) owns everything
         actually drawn once the activity's content is set. -->
    <style name="Theme.Xylotune" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
